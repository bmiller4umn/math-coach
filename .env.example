MATH_COACH_API_KEY=sk-ant-...         # Anthropic API key
ACCESS_PIN=123456                     # Student PIN (4-6 digits)
DASHBOARD_PIN=654321                  # Parent dashboard PIN (different from student PIN)
RATE_LIMIT_PER_HOUR=100              # Max API requests per IP per hour
KV_REST_API_URL=                      # Vercel KV connection (auto-populated when you link KV store)
KV_REST_API_TOKEN=                    # Vercel KV token (auto-populated)
